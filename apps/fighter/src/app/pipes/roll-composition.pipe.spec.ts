import { RollCompositionPipe } from './roll-composition.pipe';

describe('RollCompositionPipe', () => {
  it('create an instance', () => {
    const pipe = new RollCompositionPipe();
    expect(pipe).toBeTruthy();
  });
});
