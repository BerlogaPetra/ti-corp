import { createSlice } from "@reduxjs/toolkit";

const planetsSlice = createSlice({
    name: "planets",
    initialState: [],
    reducers: {
        invade: (state, action) => {
            return state
        }
    }
})